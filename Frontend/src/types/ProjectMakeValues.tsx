interface ProjectMakeValues {
    title: string;
    category: string;
    targetAmount: number;
    bestImage?: File | null;
    prevViewImage?: null;
    startDate: string;
    endDate: string;
}

export default ProjectMakeValues;