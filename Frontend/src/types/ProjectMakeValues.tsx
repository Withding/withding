interface ProjectMakeValues {
    title: string;
    category: string;
    targetAmount: number;
    bestImage?: File | null;
    previewImage?: null;
    startDate: string;
    endDate: string;
}

export default ProjectMakeValues;